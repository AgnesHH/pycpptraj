# distutils: language = c++

#not finished yet

from libc.stdlib cimport malloc 

def dec_main(funct):
     def inner(*args):
         cdef char **c_argv
         args = [str(x) for x in args]
         c_argv = <char**>malloc(sizeof(char*) * len(args)) 
         for idx, s in enumerate(args):
             c_argv[idx] = s
         funct(len(args), c_argv)
     return inner

@dec_main
def main_py(argv):
    '''argv: list of strings'''
    main(len(ag
