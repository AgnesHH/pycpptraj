from openmp cimport *
from cython.parallel cimport prange

cdef omp_sched_t test = omp_sched_guided
cdef int i
cdef int sum = 0

omp_set_num_threads(3)

sum = 0
for i in prange(100, nogil=True, schedule='static'):
    sum += i
    print 
    
print sum
