# distutils: language = c++

cdef extern from "<vector>" namespace "std":
    cdef cppclass _vector[T] "vector[T]":
        cppclass iterator:
            T operator*()
            iterator operator++()
            bint operator==(iterator)
            bint operator!=(iterator)
        vector()
        void push_back(T&)
        T& operator[](int)
        T& at(int)
        iterator begin()
        iterator end()

cdef _vector[int].iterator iter  #iter is declared as being of type vector<int>::iterator
